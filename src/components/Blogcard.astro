---
const {
  cardimage = "",
  cardimagealt = "",
  category = "",
  categoryColor = "bg-primaryblue60",
  date = "",
  author = "",
  cardtitle = "",
  cardtext = "",
} = Astro.props;
import Button from "../components/Button.astro";
import Heading from "../components/Heading.astro";
---

<div
  class="relative w-full h-full max-w-[360px] mx-auto hover:scale-[1.02] transition-transform duration-300"
>
  <!-- Outer card -->
  <div
    class="flex h-full flex-col rounded-2xl border-orange60 border-2 bg-beige p-6"
  >
    <!-- Category tag -->
    <div class="mb-2 flex">
      <span
        class={`px-4 py-1  rounded-full text-white text-sm font-medium ${categoryColor}`}
      >
        {category}
      </span>
    </div>

    <!-- Image -->
    <figure class="relative w-full">
      <img
        src={`/${cardimage}`}
        alt={cardimagealt}
        class="w-full h-56 object-cover rounded-[16px] border-2 border-orange60"
      />
    </figure>

    <!-- Text content -->
    <div class="flex flex-col justify-start items-start text-left">
      <!--  info -->
      <p class="mt-3 mb-1 text-sm">
        {date} // {author}
      </p>

      <!-- Title -->
      <div>
        <Heading htmlheading="h2" size="small">
          {cardtitle}
        </Heading>
      </div>

      <!--  card text -->
      <p class="text-sm">
        {cardtext}
      </p>
    </div>

    <!-- CTA -->
    <div class="flex justify-start">
      <Button href="#ungtilung" variant="secondary" size="small">
        Læs mere
      </Button>
    </div>
  </div>
</div>
